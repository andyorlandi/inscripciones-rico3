# Admin password for accessing /admin panel
# IMPORTANT: Change this to a secure password in production
# Generate one with: openssl rand -base64 32
ADMIN_PASSWORD=admin123

# Database URL (PostgreSQL)
# For Vercel: Use Vercel Postgres and copy the POSTGRES_URL
# For local dev: Use a local PostgreSQL instance
DATABASE_URL="postgresql://user:password@localhost:5432/inscripciones_rico3"
